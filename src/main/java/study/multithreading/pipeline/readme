

Pipeline模式
并行和并发编程中，为了提高并发，我们需要将任务分解为若干较小的任务，但小人物之间有依赖关系。Pipeline模式的每一个Pipe的
输入元素可以看作是提交给该pipe的一个任务，这样前一个pipe实例的任务处理结果就是下一个pipe实例的输入任务。从个体任务上看，
pipeline模式对各个任务的处理是顺序的。由于每个pipe实例都有其工作者线程处理，当pipe实例处理上游pipe实例提交的任务时，
上游实例已经在处理其接收到的其他任务。从整体任务上来看，pipeline对任务的处理又是并行的。

